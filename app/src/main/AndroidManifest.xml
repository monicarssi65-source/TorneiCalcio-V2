<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.App"
        android:fileProvider="@xml/file_paths">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <activity android:name=".ui.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.TournamentDetailActivity"
            android:parentActivityName=".ui.MainActivity"/>
        <activity android:name=".ui.MatchesActivity"
            android:parentActivityName=".ui.TournamentDetailActivity"/>
        <activity android:name=".ui.StandingsActivity"
            android:parentActivityName=".ui.TournamentDetailActivity"/>
        <activity android:name=".ui.ScorersActivity"
            android:parentActivityName=".ui.TournamentDetailActivity"/>
    </application>
</manifest>
